apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: helios-test

resources:
  - ../../base

# Test mirrors production's shape so that what passes here means something.
replicas:
  - name: orion-api
    count: 2
  - name: orion-worker
    count: 2
